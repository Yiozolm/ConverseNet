# s3 training study

Explicit experimental path; default dispatch remains unchanged. See [implementation and results](../../docs/training_scale3.md). Run `test/study_training_scale3.py` with a fresh artifacts directory. Serialize GPU jobs and keep profiling separate from timings.
