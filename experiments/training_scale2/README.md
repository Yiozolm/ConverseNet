# s2 training study

This is an explicit experimental path. Default build and dispatch remain unchanged.

See [the implementation and measured results](../../docs/training_scale2.md). Run the public driver `test/study_training_scale2.py` with a new `--artifacts` directory. CUDA tasks must be serialized; profiling and formal timings are separate.
