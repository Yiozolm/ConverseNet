# Converse2D 算子优化：工作现状与讨论问题

整理日期：2026-09-16。依据现有源码及已保存实验记录整理；本文未重新运行性能实验。性能表注明对应阶段，不能视为当前工作目录的统一复测结果。

## 研究对象与优化目标

我正在优化论文 *Reverse Convolution and Its Applications to Image Restoration*（ICCV 2025）中的 depthwise reverse convolution 算子 Converse2D。

论文：[arXiv 页面](https://arxiv.org/abs/2508.09824) · [PDF](https://arxiv.org/pdf/2508.09824)

该算子通过求解带先验约束的正则化最小二乘问题，从卷积及下采样后的观测恢复图像或特征：

\[
X^*=\arg\min_X\|Y-(X\otimes K)\downarrow_s\|_F^2+\lambda\|X-X_0\|_F^2.
\]

这里，Y 为观测，K 为逐通道卷积核，s 为下采样倍率，X₀ 为先验。实现利用 FFT 在频域求闭式解。我的工作主要是在保持数学语义、边界条件及梯度正确性的前提下，降低 GPU 推理和训练的时间与显存开销，并接入论文中的图像恢复模型。

## 已完成的主要工作

**数值与算法表达。** 将闭式解改写为“先验 + 残差校正”，缓解小正则项下的消减误差；修正早期版本的频率分组及平铺问题。采用 R2C/C2R 实数 FFT 和 Hermitian 半谱，减少频谱存储与访问；保留独立先验输入的语义。

用 K̂、P、Ŷ 表示核、先验与观测的频谱，A 表示倍率对应的频率块平均，E 表示相应的频率展开，核心计算可写为：

```
D = A(|K̂|²) + λ
Q = (Ŷ − A(K̂ P)) / D
Z = P + conj(K̂) E(Q)
X = inverse_FFT(Z)
```

**CUDA 融合及数据搬运。** 将频谱乘加、归约、除法和校正合并到专用 kernel；融合卷积核补零与中心移位，对动态核在校正过程中累计功率分母；对固定核缓存频谱并处理权重更新后的失效。针对较大 batch 引入按 L2 容量选择的分块策略。

**减少 FFT 和临时张量。** 对模型常用的 nearest-upsample 先验，在推理时直接由低分辨率频谱生成其频域贡献，省去显式高分辨率先验及其 FFT。推理专用 C2R 路径允许消耗内部临时频谱并直接写最终输出，减少保护性复制和中间存储，同时管理 plan、workspace、stream 与 CUDA Graph 生命周期。

**训练与混合精度。** 已实现半谱求解的融合前向和一阶反向，处理共享卷积核的梯度归约，并保留高阶梯度的可微回退。最初的性能记录针对 FP16/BF16；当前源码也已将 FP32 CUDA 训练接入该后端。低精度激活配合 FP32 主参数，默认 FFT 与频谱求解保持 FP32；另有显式开启的原生 FP16/BF16 FFT 实验路径，对不支持的条件回退。该实验路径仍需进一步评估误差与端到端收益。

**模型集成与验证。** 已接入 Converse-DnCNN、Converse-SRResNet 和 Converse-USRNet，提供固定形状 USRNet 推理的可选 CUDA Graph。已有验证覆盖 FP64/独立参考、一阶与二阶梯度、奇偶与矩形尺寸、核广播、动态核、缓存失效、多 stream、图捕获和预训练模型输出。尚未完成全部最新优化的标准数据集质量评估与完整训练收敛验证。

## 已记录的代表性结果

以下实验均来自 RTX 5060 Ti、PyTorch 2.11.0+cu130、CUDA 13.0。各行的对照基线不同，应分别解读，不能把加速比相乘。

| 阶段与测量范围 | 对照条件 | 代表性结果 |
|---|---|---|
| v1.0 固定核 FP32 算子推理 | 上游原始 Python；1×64×128×128，s=2 | 8.3898 → 0.6042 ms，13.89× |
| v1.0 完整 USRNet 推理 | 上游原始 Python；LR 64×80，B=1，s=2 | 216.296 → 43.584 ms，4.96×；启用 Graph 后 36.784 ms，总加速 5.88× |
| nearest 先验融合 | 当时已优化的显式 nearest + 独立先验路径；1×64×128×128，s=2 | 0.5925 → 0.3532 ms，耗时降低 40.4% |
| C2R 去复制与直接输出集成 | 修改前主线；8×32×128×128，s=2，nearest | 1.4189 → 0.9849 ms，耗时降低 30.6% |
| 低精度训练频谱融合 | 融合前低精度实现；FP16，1×32×64×80，s=2，独立先验 | 前向+反向 2.5446 → 1.0358 ms，2.457× |

前两行来自冻结的 v1.0 版本，后续行为独立阶段 A/B。推理为预热后的稳态时间；Graph 不含首次捕获，算子训练行不含优化器。v1.0 使用同步 wall time，后三行使用 CUDA Event 中位数。固定核缓存是否预热也属于各实验条件的一部分。

训练融合还在一个合成的“两次迭代、单恢复块”USRNet 上测得完整训练步约 1.26×（FP16）和 1.31×（BF16）的阶段收益；这不是论文完整网络的训练加速或收敛结果。

## 目前的瓶颈判断

**频谱校正有明确的访存压力。** 一次 SM120 Nsight Compute 采样中，s=2 的 alias/apply 两个 kernel 的 DRAM throughput 指标约为峰值的 95%，achieved occupancy 约为 85%–91%。该样例更支持优先减少频谱往返读写，而不是单纯提高算术吞吐。数据对应当时版本及特定输入，最新代码需要重新 profiling。

**FFT 尺寸和工作集影响很大。** 非规则尺寸可能触发明显更慢的 FFT 路径，大 batch 也可能使工作集超出缓存。直接 padding 到更适合 FFT 的尺寸会改变周期边界下的算子，因此需要先论证数学等价性，不能只比较速度。

**算子收益与整网收益不成比例。** 既有 Systems 时间线显示，小图像场景有大量短 kernel 与主机提交间隙；完整模型中恢复块也占据较大开销。因此既要研究算子内部，也要评估 LayerNorm、逐元素操作和图调度。早期时间线的占比不直接代表最新版本。

**减少一次 kernel 启动不一定能提速。** 已评估将归一化放入 cuFFT LTO store callback；260×260 等个别形状有收益，但部分形状会换用更慢的 FFT 实现，并增加计划构建成本。目前没有将该方案作为默认路径。

## 希望请教的问题

1. **频谱融合的边界如何选择？** alias 归约和频谱更新进一步融合时，如何权衡减少全局内存读写与重复计算、寄存器压力、并行度下降？是否适合尝试 shared-memory 分块或 warp 协作？
2. **如何组织 FFT 与工作集？** 现有按 batch 分块的启发式是否合理，是否应进一步按通道、尺寸和设备选择策略？哪些规模值得尝试 cuFFTDx 等更深层融合？
3. **训练端下一步优先做什么？** 已有频谱前向/反向融合之后，FFT、广播梯度归约、临时张量和恢复块融合中，哪一部分最值得投入？
4. **如何建立更有说服力的性能分析？** 希望指导我们设计覆盖不同尺寸、batch、静态/动态核和训练/推理的 benchmark，结合 roofline、内存流量与完整时间线，区分启动、带宽、计算和 FFT 库实现带来的限制。

下一步计划先冻结当前源码和环境，统一复测算子及整网前向/训练步，再对收益最大的候选做单变量 A/B，并补齐标准图像集质量与训练收敛评估。

## 实验依据与阅读入口

- [v1.0 与上游原始 Python 对照](artifacts/releases/original_python/release_revised.md)
- [nearest 先验融合](artifacts/nearest_fused/report.md)
- [C2R 直接输出集成](artifacts/b_direct_integration/report.md)
- [低精度训练融合及验证范围](test/TRAINING_FUSION.md)
- [SM120 性能计数器调查](artifacts/sm120_investigation/report.md)
- [Nsight Systems 分析](artifacts/nsight_optimization/optimization_report.md)
- [LTO callback 实验及未默认采用的原因](artifacts/c2r_lto/report.md)

当前源码中的 FP32 训练接入与原生低精度 FFT 已超出部分阶段文档的记录范围；当前工作目录也包含尚未纳入 Git 的实现文件。若共享复现代码，需要提供包含这些文件的固定快照，不能仅以当前 HEAD 或早期发布标签代表本文全部实现。
